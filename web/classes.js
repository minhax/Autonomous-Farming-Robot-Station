function Field(length, width, rows, space, type , humidity) = {
  length = length;
  width = width;
  rows = rows;
  spacingBetweenRows = space;
  type = type;
  humidity = humidity;
}

function Point(x,y, number) = {
  x = x;
  y = y;
  number = number;
}

function Robot(uuid,status,equipment) {
  uuid = uuid;
  status = status;
  equipment = equipment;
}
